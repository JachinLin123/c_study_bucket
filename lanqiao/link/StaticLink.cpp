//静态链表，不需要头结点，有些链表比较小，所以没有必要弄动态数组 
#include <bits/stdc++.h>
#include <stdlib.h>
#include <stdio.h>

using namespace std;

const int size = 5;

struct Node { //node info
	int data;
	int next;
}Node[size];

int main() {

	return 0;
}
